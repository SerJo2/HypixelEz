# Hypixelez
### [English](](https://github.com/SerJo2/hypixelez/blob/master/README.md)) | Russian




Современная Python библиотека для удобного взаимодействия с Hypixel API, специально разработанная для работы с данными SkyBlock.
## Возможности

- 🔍 **Поиск UUID** - Получение UUID игрока по имени Minecraft
- 📊 **Полная статистика SkyBlock** - Доступ к коллекциям, навыкам, слайерам и подземельям
- 🎯 **Типизация** - Полная поддержка типов для лучшего опыта разработки
- 🛡️ **Обработка ошибок** - Надежная обработка ошибок с безопасными значениями по умолчанию
- 📝 **Логирование** - Настраиваемое логирование для отладки
- ⚡ **Кэширование** - Умное кэширование для поиска UUID
- 🧪 **Тестирование** - Полный набор тестов

## Установка

```bash
pip install hypixelez
```

## Документация
[Docs](https://serjo2.github.io/HypixelEz/)

### Сборка документации
`python -m sphinx -b html docs\source docs\build\html`

## Быстрый старт
``` python
from hypixelez import HypixelClient

# Инициализация клиента с API ключем
client = HypixelClient(api_key="your-hypixel-api-key")

# Получение UUID
uuid = client.get_uuid_by_name("Neono4ka")

# Получение профилей скайблока
profiles = client.get_profile_names_ids_by_id(uuid)
# {'Kiwi': '0b1362a7-43e8-454b-a2ed-6db43ae32f19', ...}

# Получение информации о профиле
profile_data = client.fetch_profile_info(uuid, "f5791b0c-caf1-4701-aea3-d727ea53a901")

# Доступ к разной статистике
print(f"Skill Level: {profile_data.get_skill_level('SKILL_CARPENTRY')}")
print(f"Collection: {profile_data.get_collection('LOG')}")
print(f"Slayer XP: {profile_data.get_slayer_xp('zombie')}")
print(f"Catacombs Level: {profile_data.get_cata_level()}")
```

## Доступные методы
### Информация об игроке
```get_global_level()``` - Уровень скайблока

```get_global_xp()``` - Текущий опыт скайблока

### Скилах
```get_skill_level(skill_name)``` - Уровень скила (0-60)

```get_skill_current_level_xp(skill_name)``` - Текущий опыт скила

### Коллекции
```get_collection(collection_name)``` - Коллекции

### Слееры
```get_slayer_stats(slayer_name)``` - Список убийств тиров слеера

```get_slayer_stats_by_tier(slayer_name, tier)``` - Убийства определенного тира слеера

```get_slayer_xp(slayer_name)``` - Опыт слеера

```get_slayer_level(slayer_name)``` - Уровень слеера

### Данжи
```get_cata_level()``` - Уровень катакомб

```get_cata_xp()``` - Опыт катакомб

```get_cata_class_level(class_name)``` - Уровень класса

```get_cata_class_xp(class_name)``` - Опыт класса

## Конфигурация
``` python
# Включение дебаг логинга
client = HypixelClient(api_key="your-key", debug=True)

# Отклбчение дебаг логинга (default)
client = HypixelClient(api_key="your-key", debug=False)
```

## Обработка ошибок
Все методы возвращают безопасные значения по умолчанию (обычно 0), когда данные не найдены:

```python
# Если навык не существует, возвращает 0 вместо ошибки
level = profile_data.get_skill_level("NON_EXISTENT_SKILL")  # Возвращает 0
```

## Получение API ключа
1. Посетите Hypixel Developer Dashboard
2. Войдите с помощью вашего аккаунта Minecraft
3. Сгенерируйте новый API ключ
4. Используйте его в вашем приложении

## Тестирование 
Для запуска тестов установите ваш API ключ как переменную окружения:

```bash
export API_KEY="your-hypixel-api-key"
pytest
```

## Требования 
 - Python 3.9+
 - requests library

## Лицензия
MIT License - подробности в файле LICENSE.

## Участие в разработке
Мы приветствуем участие в разработке! Пожалуйста, не стесняйтесь отправлять pull requests или создавать issues для багов и запросов функций.

## Поддержка
Если вы столкнулись с проблемами или у вас есть вопросы, пожалуйста, создайте issue на GitHub.

## ⭐ If this project helped you, please give it a star on GitHub!